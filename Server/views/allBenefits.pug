extends base

block content
    #container
        h1= title

            
        #benefits
            #cardReview 

                each benefit in benefits

                    .card 
                        img(src='img/icon.png', alt='Icone')
                        .article 
                            h3= benefit.title  
                            a(href=`/les-prestations/${benefit.slug}`) En savoir plus
                        
                